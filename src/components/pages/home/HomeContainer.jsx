import { useState } from "react";
import Home from "./Home";

const HomeContainer = () => {
  //sumar={sumar} contador={contador} setContador={setContador}///

  /*const sumar = () => { setContador(contador + 1); };*/
  return <Home />;
};

export default HomeContainer;
